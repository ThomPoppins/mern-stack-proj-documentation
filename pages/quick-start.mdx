import PrimaryButton from '../components/buttons/PrimaryButton'

# Quick Start Guide

Follow these simple steps to set up the application locally.

If you do not want to set up and test this project locally, you can skip this and proceed directly to the Interactive Demo section.

<a href='/interactive-demo'>
  <PrimaryButton>Interactive Demo</PrimaryButton>
</a>

## Setting Up Your Environment

### Prerequisites:

#### MongoDB

MongoDB, a NoSQL database, allows you to store data in JSON-like documents, a great choice for Node.js applications.

##### Register a MongoDB account and get a free database:

import MongoDBTryFreeButton from '../components/buttons/MongoDBTryFreeButton'

<a href='https://www.mongodb.com/' target='_blank'>
  <MongoDBTryFreeButton>Get Free Database</MongoDBTryFreeButton>
</a>

#### Git

Git, a version control system, lets you track changes to files and coordinate work among multiple contributors.

If you choose not to install git, no worries—you can still set up the application locally.

##### Download:

import GetGitButton from '../components/buttons/GetGitButton'

<a href='https://git-scm.com/downloads' target='_blank'>
  <GetGitButton>Get Git</GetGitButton>
</a>

#### Terminal

For this application, you need a terminal to start the servers.

- On Windows, use Git Bash or Powershell.
- On Mac or Linux, Git Bash works as well.

#### Node.js + NPM:

Node.js is a JavaScript runtime, and NPM is a package manager for Node.js, automatically installed with Node.js.

##### Download

import NodeButton from '../components/buttons/NodeButton'

<a href='' target='_blanc'>
  <NodeButton>Get Node.js</NodeButton>
</a>
Get Node.js

### Fetch the Code

You can choose to clone the repository or download the code as a zip file.

#### Clone the Repository

```bash filename="Bash" copy
git clone git@github.com:ThomPoppins/MERN_STACK_PROJ.git MERN_STACK_PROJ && cd MERN_STACK_PROJ
```

#### Download the Code

import DownloadButton from '../components/buttons/DownloadButton'

<a href='https://github.com/ThomPoppins/MERN_STACK_PROJ./archive/refs/heads/main.zip'>
  <DownloadButton>Download Code</DownloadButton>
</a>

### Backend Configuration

1. Navigate to the `/backend` folder in your file explorer.
2. Create a `config.js` file.
3. Add the following constants and update them with your personal values:

   ```javascript filename="Configuration: /backend/config.js" {3} copy
   export const PORT = 5555
   export const mongoDBURL = 'your-mongodb-connection-url'
   export const JWT_SECRET = 'your-secret-key'
   export const KVK_TEST_API_KEY = 'your-test-api-key'
   export const KVK_PROD_API_KEY = 'your-prod-api-key'
   ```

### Frontend Configuration

1. Navigate to the `/frontend` folder.
2. Create a `config.js` file if it doesn't exist.
3. Add the following constant and export it:

   ```javascript filename="Configuration: /frontend/config.js" {3} copy
   export const BACKEND_URL = 'http://localhost:5555'
   export const TEST_KVK_API = false
   ```

### Install Dependencies

- Inside the `/backend` folder, run:

  ```bash copy
  npm install
  ```

- Inside the `/frontend` folder, run:

  ```bash copy
  npm install
  ```

### Start Servers

- Inside the `/backend` folder, run:

  ```bash copy
  npm run dev
  ```

- In a separate terminal, inside the `/frontend` folder, run:

  ```bash copy
  npm run dev
  ```

### Access the Application

Visit the web application in your browser using the link provided by the Vite.js server after starting the frontend server.

**Now you have the application up and running locally!**
